
import torch
# print(torch.cuda.is_available())
# device = torch.device('cuda:0' if torch.cuda.is_available() else 'cpu')
# print(f'Using device: {device}')

# print(torch.__version__)
# print(torch.version.cuda)  # 显示 PyTorch 编译使用的 CUDA 版本
